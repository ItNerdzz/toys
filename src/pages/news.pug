extends /layouts/main.pug

block variables
  - const title = "News" //- на страницах кроме главной используйте О нас — ProjectName
  - const wrapperMods = ""
  - const hasHeader = true
  - const headerMods = ""
  - const hasFooter = true
  - const footerMods = ""
  - const currentUrl = "news.html" //- для остальных страниц пишем пустую строку или индекс активного headerLinks


//- block pageContent

block main
  main.news
    -
      const nameIitemsBreadcrumbs = [
        "Новости",
      ];

    +breadcrumbs(nameIitemsBreadcrumbs).news__breadcrumbs
    +news-block().news__news-block
    +viewed()
